<?xml version="1.0" encoding="utf-8" ?>
<TabbedPage  xmlns="http://xamarin.com/schemas/2014/forms"
					   xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
					   x:Class="LunchAndLearnMobile.Classes">

  <TabbedPage.Children>
    <ContentPage Title="Classes">

      <StackLayout Orientation="Vertical" VerticalOptions="Start">
        <ActivityIndicator IsVisible="{Binding IsLoading}" IsRunning="{Binding IsLoading}" HorizontalOptions="CenterAndExpand" />

        <ListView ItemsSource="{Binding Classes}">
          <ListView.ItemTemplate>
            <DataTemplate>
              <TextCell Text="{Binding Title}" Detail="{Binding Description}" />
            </DataTemplate>
          </ListView.ItemTemplate>
        </ListView>
      </StackLayout>
    </ContentPage>

    <!--<ContentPage Title="Instructors">
      <StackLayout Orientation="Vertical" VerticalOptions="FillAndExpand">
        <ListView ItemsSource="{Binding Instructors}">
          <ListView.ItemTemplate>
            <DataTemplate>
              <TextCell Text="{Binding Title}" Detail="{Binding Description}" />
            </DataTemplate>
          </ListView.ItemTemplate>
        </ListView>
      </StackLayout>
    </ContentPage>-->
  </TabbedPage.Children>

</TabbedPage>